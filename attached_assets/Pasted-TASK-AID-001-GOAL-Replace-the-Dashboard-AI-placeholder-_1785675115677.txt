TASK: AID-001

GOAL

Replace the Dashboard AI placeholder with a real Dashboard AI aggregator.

IMPORTANT

Dashboard AI is NOT an AI engine.

Do NOT create new AI logic.

Do NOT generate predictions.

Do NOT create another rule engine.

==================================================

Dashboard AI must ONLY aggregate AI Insight produced by existing modules.

Current AI modules already exist:

- AI Bobot
- AI Pakan
- AI Kesehatan
- AI Livestock
- AI Batch
- AI Mutasi
- AI Reproduksi

==================================================

Replace:

src/data/aiInsightData.ts

The placeholder AI_INSIGHT_DUMMY_LIST

with real aggregation.

==================================================

Requirements

Dashboard AI must display the existing insight generated by AI modules.

Do NOT duplicate AI rules.

Do NOT rewrite AI engines.

Do NOT move business logic.

Each module remains responsible for its own analysis.

Dashboard only combines, sorts, groups and displays them.

==================================================

Preserve the existing Dashboard UI.

Do NOT change:

- layout
- cards
- grouping
- colors
- icons
- navigation

==================================================

If one AI module currently has no available insight because its module has no data yet,

skip it gracefully.

Do NOT create fake insight.

==================================================

If an adapter is required,

reuse the existing public APIs exported by each AI module.

Do NOT modify AI engines unless absolutely required for compatibility.

==================================================

Before finishing

npm run build

==================================================

Output

1. Files modified

2. AI modules aggregated

3. Any module skipped because no real data exists

4. Whether aiInsightData.ts placeholder was removed

5. Build status

6. TypeScript status

7. ESLint status