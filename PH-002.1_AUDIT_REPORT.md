# PH-002.1 — Cloudflare Pages Control Panel: Audit Report

**Date:** 2026-08-04  
**Task:** PH-002.1 — Finalize Cloudflare Pages Control Panel  
**Status:** ✅ Complete

---

## 1. Field Audit: REAL RUNTIME

Hanya **satu** field yang genuinely bisa dibaca dari browser tanpa Cloudflare API token:

| Field | Source | Value |
|-------|--------|-------|
| **HTTPS** | `window.location.protocol === 'https:'` | Status koneksi TLS session saat ini — bukan CF project config, tapi browser state yang nyata |

Field ini muncul di dua section (Domain dan Security) karena keduanya menanyakan hal yang sama dari sudut pandang berbeda. Nilainya identik dan jelas diberi catatan bahwa ini adalah session browser, bukan konfigurasi CF.

---

## 2. Field yang Managed by Cloudflare Dashboard

Semua field di bawah ini memerlukan Cloudflare API token (`CLOUDFLARE_API_TOKEN`) yang **tidak boleh** diekspos ke browser. Setiap hint menunjukkan endpoint atau lokasi dashboard yang tepat.

### General
| Field | CF API Path |
|-------|-------------|
| Project Name | `GET /accounts/:id/pages/projects/:name` |
| Production URL | `project.canonical_deployment.url` |
| Preview URL | Per-branch preview URLs |
| Custom Domain | Dashboard → Pages → Custom Domains |
| Production Branch | `project.production_branch` |
| Framework | `project.build_config.destination_dir` |
| Deployment Status | `deployment.latest_stage.status` |

### Build
| Field | CF API Path |
|-------|-------------|
| Build Command | `project.build_config.build_command` |
| Output Directory | `project.build_config.destination_dir` |
| Node Version | Dashboard → Pages → Settings → Environment Variables (NODE_VERSION) |
| Environment | `project.deployment_configs.production/preview` |
| Last Build | `deployment.created_on` |
| Build Duration | `deployment.build_time_ms` |

### Deployment
| Field | CF API Path |
|-------|-------------|
| Deployment ID | `deployment.id` |
| Commit SHA | `deployment.deployment_trigger.metadata.commit_hash` |
| Commit Message | `deployment.deployment_trigger.metadata.commit_message` |
| Deploy Time | `deployment.created_on` |
| Rollback | `POST /accounts/:id/pages/projects/:name/deployments/:id/rollback` |

### Domain
| Field | CF Location |
|-------|-------------|
| pages.dev URL | `project.subdomain` |
| Custom Domain | Dashboard → Pages → Custom Domains |
| SSL | Dashboard → SSL/TLS → Edge Certificates |
| DNS | Dashboard → DNS → Records |

### Cache
| Field | CF Location |
|-------|-------------|
| Cache | Dashboard → Caching → Configuration |
| Asset Cache | Dashboard → Caching → Cache Rules |
| Browser Cache | Dashboard → Caching → Browser Cache TTL |
| Purge Cache | `POST /zones/:zone_id/purge_cache` |

### Security
| Field | CF Location |
|-------|-------------|
| Headers | Dashboard → Rules → Transform Rules → Modify Response Header |
| HSTS | Dashboard → SSL/TLS → Edge Certificates → HTTP Strict Transport Security |
| Access Policy | Cloudflare Zero Trust → Access → Applications |

### Monitoring
| Field | CF API Path |
|-------|-------------|
| Last Deploy | `deployment.created_on` |
| Deploy Result | `deployment.latest_stage.status` |
| Deploy Errors | `deployment.stages[].status = failed` |
| Deploy Duration | `deployment.build_time_ms` |

---

## 3. Field yang Sebelumnya Salah Menggunakan Data Lokal (PH-002 → Diperbaiki di PH-002.1)

| Field | Sumber Salah di PH-002 | Sumber yang Benar |
|-------|------------------------|-------------------|
| **Build Command** | `package.json` → `scripts.build` (`npm run build`) | Cloudflare Pages API: `project.build_config.build_command` |
| **Output Directory** | `vite.config.ts` → `build.outDir` (`dist`) | Cloudflare Pages API: `project.build_config.destination_dir` |
| **Node Version** | `package.json` → `engines.node` (`>=20.0.0`) | Cloudflare Dashboard → Pages → Settings → `NODE_VERSION` env var |
| **Framework** | Hardcoded string `React 18 + Vite` inferred dari deps | Cloudflare Pages API: `project.build_config` (CF-detected framework) |
| **SPA Routing** | `public/_redirects` file content | Bukan CF Pages control plane data |
| **Environment (Vite mode)** | `import.meta.env.MODE` (Vite runtime) | Cloudflare Pages API: `project.deployment_configs` |
| **Production URL** | `window.location.href` (Replit dev URL, bukan CF URL) | Cloudflare Pages API: `project.canonical_deployment.url` |

**Kesimpulan:** Semua field di atas **menampilkan data lokal** yang secara kebetulan mirip dengan konfigurasi CF Pages, tetapi bukan data dari Cloudflare. Ini adalah Build Viewer, bukan Control Panel. PH-002.1 menghapus semua nilai tersebut dan menggantinya dengan `Managed by Cloudflare Dashboard` beserta hint yang menunjukkan CF API path yang tepat.

---

## 4. Perubahan yang Dilakukan

**File:** `src/pages/admin/modules/PlatformHealthModule.tsx`

- Dihapus: variabel `BUILD_CMD`, `OUTPUT_DIR`, `NODE_VER`, `FRAMEWORK`, `SPA_ROUTING` (semua dari file lokal)
- Dihapus: `const envMode = import.meta.env.MODE` (Vite mode, bukan CF config)
- Dihapus: `const currentUrl = window.location...` (Replit dev URL, bukan CF Pages URL)
- Dihapus: konstanta `CF_UNAVAIL` dan `CF_RO` (semua field kini seragam: `CF_DASH`)
- Dipertahankan: `isHttps = window.location.protocol === 'https:'` (satu-satunya real runtime value)
- Semua field yang sebelumnya terisi dari data lokal → sekarang menampilkan `Managed by Cloudflare Dashboard`
- Setiap field dilengkapi `hint` berisi exact CF API endpoint atau Dashboard path

---

## 5. TypeScript

```bash
./node_modules/.bin/tsc -b --pretty false
```

**Result: 0 errors** ✅

---

## 6. ESLint

```bash
./node_modules/.bin/eslint src/pages/admin/modules/PlatformHealthModule.tsx
```

**Result: 0 warnings, 0 errors** ✅

---

## 7. Konfirmasi

✅ **Cloudflare Pages drawer sekarang merepresentasikan Control Plane, bukan Build Viewer.**

- Tidak ada satu pun field yang diisi dari `package.json`, `vite.config.ts`, `_redirects`, atau `import.meta.env`
- Satu-satunya nilai real adalah HTTPS status dari `window.location.protocol` — genuine browser runtime state
- Semua field lainnya jujur: `Managed by Cloudflare Dashboard` dengan hint yang menunjukkan lokasi persis di CF Dashboard atau CF API endpoint
- Drawer berfungsi sebagai **referensi Control Plane** — admin tahu persis di mana setiap setting dikelola
